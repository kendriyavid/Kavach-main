import 'package:flutter/material.dart';

class messages extends StatelessWidget {
  const messages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Message Access krna hai"),
      ),
    );
  }
}
